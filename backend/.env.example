POSTGRES_USER="secrep"
POSTGRES_PASSWORD="change_me"
POSTGRES_DB="secrep_db"
DATABASE_URL="postgresql://secrep:change_me@localhost:5432/secrep_db?schema=public"
FRONTEND_URL="http://localhost:3000"
PORT=4000

JWT_SECRET="change-me"
JWT_REFRESH_SECRET="change-me"

COOKIE_SAMESITE="lax"
COOKIE_SECURE="0"
COOKIE_DOMAIN=""

SMTP_HOST="smtp.your-provider.com"
# Обычно: 587 (STARTTLS) или 465 (SSL). Если 465 блокируется сетью — используйте 587.
SMTP_PORT="587"
SMTP_SECURE="0"
SMTP_USER="no-reply@your-domain.com"
SMTP_PASS="your-smtp-password"
SMTP_FROM="no-reply@your-domain.com"
SMTP_FROM_NAME="SecRep"
SMTP_DEV_MAILHOG="1"

RESEND_API_KEY=""
RESEND_FROM="onboarding@resend.dev"
RESEND_FROM_NAME="SecRep"

OTP_DEBUG_RETURN_CODE="0"
